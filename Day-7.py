"""Given a list of words, find all pairs of unique indices such that the concatenation of
the two words is a palindrome.
For example, given the list [ 11 code 11,11 edoc 11,11 da 11,11 d 11],
return [ ( 0, 1), ( 1,0), (2, 3)]."""